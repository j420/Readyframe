# Three-minute demo

1. Run `make demo`.
2. Show the 512 readiness score, wide confidence interval, evidence, and counterfactual.
3. Show the rollback `DENY` rule and its NIST control/evidence `because` trace.
4. Point to `ROLLBACK FIRED`: pre-execution denial and compensating revert are different, honest mechanisms.
5. Show v1→v2: holdout improvement, rollback weight diff, same customer 512→547, then poison refusal.
6. Close on network value: accepted prior deployments improve the next score without exposing customers.
