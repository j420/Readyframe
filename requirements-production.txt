# Production-only dependencies.  Local deterministic tests intentionally use stdlib.
psycopg[binary]>=3.1.18,<4

# OIDC signature verification (pinned for repeatable production authentication).
PyJWT[crypto]==2.13.0
cryptography==49.0.0
